{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx vercel dev"
  },
  "dependencies": {
    "@vercel/node": "^2.8.17"
  },
  "devDependencies": {
    "vercel": "^28.14.1"
  }
}
